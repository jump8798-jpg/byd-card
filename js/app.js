const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
let idx=0;const reviews=[...document.querySelectorAll('.review')];const dots=document.getElementById('dots');if(dots){dots.innerHTML=reviews.map((_,i)=>`<span class="${i===0?'active':''}"></span>`).join('')}function showReview(n){reviews.forEach((r,i)=>r.classList.toggle('active',i===n));[...dots.children].forEach((d,i)=>d.classList.toggle('active',i===n))}setInterval(()=>{idx=(idx+1)%reviews.length;showReview(idx)},3500);
function calc(){const eff=+document.getElementById('eff').value||12;const fuel=+document.getElementById('fuel').value||1650;const km=+document.getElementById('km').value||20000;const years=+document.getElementById('years').value||5;const gas=km/eff*fuel;const ev=km*75;const tax=160000;const save=(gas-ev+tax)*years;document.getElementById('result').innerHTML=years+'년 예상 절감액<br><b style="font-size:30px">약 '+Math.round(save).toLocaleString()+'원</b>'}